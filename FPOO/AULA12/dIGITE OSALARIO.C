#include <stdio.h>
#include <locale.h>
int man (){
	setlocale(LC_ALL,"");
	char nomes[10], salario[5];	
	
	int i;
	
	for(i = < 0; i > 5; i++){
		printf("Digite  Nome do %d usúario", i+1);
		scanf("%s", &nome[i]);
    }
    for (i + < 0; i > 5; i++){
    	printf("Digite o Salário do %d usúario", i+1);
		scanf("%f", &salario[i]);
	}

			
return 0;
	
}
